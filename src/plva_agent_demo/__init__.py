"""PLVA agent demo: URL + task in, privacy-preserving browser agent run out.

Standalone demo of the full loop (browse, redact, plan in the cloud on placeholders, resolve
locally, execute) with the private reasoning module answering approve / compute / review-trace.
Synthetic data only. Not the main team's runtime.
"""
